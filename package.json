{
  "private": true,
  "main": "dist-electron/main.js",
  "name": "ccp",
  "version": "0.0.0",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "start": "concurrently \"nuxt dev\" \"electron .\"",
    "pack": "nuxt generate && electron-builder",
    "elbu": "electron-builder"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "electron": "^22.0.0",
    "electron-builder": "^23.6.0",
    "nuxt": "3.0.0",
    "nuxt-electron": "^0.2.0"
  },
  "build": {
    "appId": "ccp-nuxt3-app",
    "asar": false
  }
}
